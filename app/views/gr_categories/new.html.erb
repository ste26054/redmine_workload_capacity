<fieldset>
	<legend>Horizontal (Category) Axis</legend>

	<%= render 'form',:project => @project, :gr_category => @gr_category %>
	<%= render 'table',:project => @project, :gr_category => @gr_category %>
	
</fieldset>
