$('#ajax-modal').html('<%= escape_javascript(render :partial => '/wl_user_overtimes/new', 
	locals: {:user => @user, :project => @project, :user_overtime => @user_overtime, :leave_list => @leave_list}) %>');
showModal('ajax-modal', '90%');
