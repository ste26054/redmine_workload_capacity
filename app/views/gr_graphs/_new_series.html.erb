

<fieldset>
	<legend>Legend Entries (Series)</legend>

	 <%= render 'form_series',:project => @project, :series => @series %>

	<%= render 'table_series',:project => @project, :series => @series %>

</fieldset>
