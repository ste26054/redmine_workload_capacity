<fieldset>
	<legend>Horizontal (Category) Axis</legend>

	<%= render 'form_category',:project => @project, :gr_category => @gr_category %>
	<%= render 'table_category',:project => @project, :gr_category => @gr_category %>
	
</fieldset>
