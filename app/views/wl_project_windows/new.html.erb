<h2>Define a new Project Window for <%= @project.name %></h2>

<%= render 'form', :project => @project , :project_window => @project_window, :available_project_roles => @available_project_roles %>
