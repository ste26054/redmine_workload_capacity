<h2>Workload Dashboard for <%= link_to 'Project Window', edit_project_wl_project_window_path(@project) %> from <%= format_date(@project.wl_project_window.start_date) %> to <%= format_date(@project.wl_project_window.end_date) %></h2>

<%= render 'index', :project => @project %>