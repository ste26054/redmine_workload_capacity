<h2>Workload Dashboard for <%= link_to @project.name, edit_project_wl_project_window_path(@project) %> from <%= format_date(@project.wl_project_window.start_date) %> to <%= format_date(@project.wl_project_window.end_date) %></h2>


	<p><strong>Notice:</strong> <strong>"*"</strong> symbol in a Project Allocation column means a user has one or several overtimes during the time period considered.</p>


<%= render 'index', :project => @project, :wl_members => @wl_members %>